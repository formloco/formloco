import { TruncateStraightPipe } from './truncate-straight.pipe';

describe('TruncateStraightPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateStraightPipe();
    expect(pipe).toBeTruthy();
  });
});
